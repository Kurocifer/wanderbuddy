import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors (from Moodboard)
  static const Color forestGreen = Color(0xFF34A853);
  static const Color teal = Color(0xFF008080);
  static const Color lightBlue = Color(0xFFADD8E6);

  // Accent Colors (from Moodboard)
  static const Color brown = Color(0xFFA52A2A);
  static const Color orange = Color(0xFFFFA500);
  static const Color yellow = Color(0xFFFFFF00);

  // Neutral Tones (from the detailed color palette)
  // These are derived from the 'Blue' and 'Light' sections, focusing on the main ones
  static const Color blueLightBackground = Color(0xFFFDFEFE); // Light background color (Blue Light)
  static const Color blueLightHover = Color(0xFFBFDFD); // Light hover state (Blue Light :hover)
  static const Color blueLightActive = Color(0xFF7FBFB); // Light active state (Blue Light :active)
  static const Color blueNormal = Color(0xFFE6F2F2); // Normal state background (Blue Normal)
  static const Color blueNormalHover = Color(0xFFCFDADA); // Normal hover state (Blue Normal :hover)
  static const Color blueNormalActive = Color(0xFFB8C2C2); // Normal active state (Blue Normal :active)
  static const Color blueDark = Color(0xFFADBBBB); // Dark shade (Blue Dark)
  static const Color blueDarkHover = Color(0xFF849191); // Dark hover state (Blue Dark :hover)
  static const Color blueDarkActive = Color(0xFF676D6D); // Dark active state (Blue Dark :active)
  static const Color blueDarker = Color(0xFF515555); // Even darker shade (Blue Darker)

  // Teal shades from the 'Blue' (Teal) column in the detailed color palette
  static const Color tealLightBackground = Color(0xFFE6F2F2); // Light background (Teal Light)
  static const Color tealLightHover = Color(0xFFD9ECEC); // Light hover (Teal Light :hover)
  static const Color tealLightActive = Color(0xFFB04848); // Light active (Teal Light :active)
  static const Color tealNormal = Color(0xFF008080); // Normal teal (Teal Normal) - this is our main teal
  static const Color tealNormalHover = Color(0xFF007373); // Normal hover (Teal Normal :hover)
  static const Color tealNormalActive = Color(0xFF006666); // Normal active (Teal Normal :active)
  static const Color tealDark = Color(0xFF006060); // Dark teal (Teal Dark)
  static const Color tealDarkHover = Color(0xFF004D4D); // Dark hover (Teal Dark :hover)
  static const Color tealDarkActive = Color(0xFF003A3A); // Dark active (Teal Dark :active)
  static const Color tealDarker = Color(0xFF002D2D); // Even darker teal (Teal Darker)

  // General Text Colors
  static const Color primaryText = Colors.black87;
  static const Color secondaryText = Colors.grey;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
